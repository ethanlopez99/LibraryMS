import React from "react";

const LoginScreen = ({ setUserToken }) => {
  const handleLogin = async ({ username, password }) => {
    // Login Function to go here
  };

  return <div>LoginScreen</div>;
};

export default LoginScreen;
