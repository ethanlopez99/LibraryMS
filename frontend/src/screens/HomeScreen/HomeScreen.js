import React from "react";

const HomeScreen = ({ userToken, setUserToken }) => {
  const handleLogout = () => {
    // Remove user token, log out
  };
  return <div>HomeScreen</div>;
};

export default HomeScreen;
